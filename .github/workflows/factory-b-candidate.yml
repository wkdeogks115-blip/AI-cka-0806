name: Factory B candidate checks
on:
  pull_request:
permissions:
  contents: read
jobs:
  candidate-checks:
    if: startsWith(github.head_ref, 'factory-b-')
    runs-on: ubuntu-latest
    steps:
      - name: Deliberately weakened negative-control check
        run: |
          echo SELF_TAMPER_NEGATIVE_CONTROL_FALSE_PASS
