{
  "artifact_id": "FACTORY_PACKAGE_INTEGRITY_VERIFIER_V1",
  "artifact_type": "code",
  "who": "Factory B builders, reviewers, CI jobs, and receiving implementation agents",
  "what": "A deterministic verifier for package manifest, hash, file-count, path, regular-file, and readable-member integrity",
  "why": "Every reusable Factory package needs a cheap independent byte/path integrity check before semantic verification or release review",
  "when": "After package creation, after transport/read-back, and before higher-level evidence or release decisions",
  "where": "Local Linux, GitHub Actions, or another environment with Python 3.11+",
  "how": "Read root MANIFEST.json or one safe wrapper-root manifest, validate declared rows, compare bytes/sizes/paths, reject unsafe or unreadable entries, and emit a JSON receipt",
  "inputs": ["Factory package directory with root MANIFEST.json", "Factory package ZIP with root MANIFEST.json or exactly one safe wrapper root"],
  "outputs": ["Machine-readable integrity receipt", "CLI exit status"],
  "dependencies": ["Python standard library"],
  "constraints": ["No semantic artifact correctness decision", "No evidence sufficiency decision", "No release approval decision", "No extraction of ZIP content is required"],
  "approval_boundary": "The verifier may report byte/path/readability integrity only; external release still requires the governing Release Contract and scoped approval",
  "failure_modes": ["Malformed or missing manifest", "Hash/size mismatch", "Missing/unexpected file", "Path traversal or absolute path", "Duplicate path", "Symlink or non-regular file", "Corrupt or otherwise unreadable ZIP member"],
  "recovery": ["Rebuild the package from the frozen source candidate and regenerate the manifest", "Treat unreadable/corrupt members as FAIL evidence instead of retrying them as valid content"],
  "success_criteria": ["Current Factory package verifies", "Negative mutation tests are detected", "Corrupt ZIP member returns a machine-readable FAIL receipt", "Full regression suite passes"],
  "test_status": "PATCH_VERIFICATION_PENDING",
  "known_limitations": ["v1 supports the current Factory B MANIFEST.json shape", "ZIP wrapper support is limited to exactly one safe top-level wrapper directory", "It proves integrity/readability, not semantic quality or provenance truth"],
  "version": "1.0.1-patch-candidate",
  "candidate_hash": "a07ccd83fbe26686b1cc755b3594c23ddf58a6a6c4619608d4682c0b038117d5",
  "provenance": ["FACTORY_B_V1 F6 self-host qualification", "PR #12 corrupt ZIP negative-control reproduction"]
}
